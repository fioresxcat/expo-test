import React, { useState, useEffect } from 'react';
import { View, Image } from 'react-native';

const BlankScreen = () => {
    // const [imageSource, setImageSource] = useState(null);

    // useEffect(() => {
    //     const storage = new Storage({ keyFilename: "social-network-9b13f-0321d923c18a.json" });
    //     const bucket = storage.bucket("social-network-9b13f.appspot.com");
    //     const file = bucket.file("example-image.jpg");

    //     file.download().then(data => {
    //     setImageSource({ uri: data[0].toString() });
    //     });
    // }, []);

    // return (
    //     <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
    //     {imageSource ? (
    //         <Image source={imageSource} style={{width: 200, height: 200}} />
    //     ) : (
    //         <Text>Loading image...</Text>
    //     )}
    //     </View>
    // );
    return (
        <View>

        </View>
    )
};

export default BlankScreen;